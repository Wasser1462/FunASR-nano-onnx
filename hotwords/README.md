Hotword module demo for ASR inference. It is shared as a reference sample rather than a production solution, 
so please verify performance (accuracy, latency, robustness) with your own data and use cases. 
The current logic is still evolving; a downstream LM-based decision layer will be added to refine final outputs.
